# Sample Input

Place a sample paper here for testing:

- `paper.pdf` — a short academic paper in PDF format
- Or `paper.tex` + figures/ — a LaTeX project

Then run:

```bash
paper2patent examples/sample_input/paper.pdf --dry-run
```

Check `patent_output/intermediate/` for the extracted structure.
